global:
  scrape_interval: 2s # Fast interval for local development so dashboards update quickly

scrape_configs:
  - job_name: 'playprom-metrics'
    static_configs:
      - targets: ['statsd-exporter:9102']
